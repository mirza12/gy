@{
    ViewBag.Title = "AppStocker";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <script src="~/Scripts/jquery-1.7.1.js"></script>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.js"> </script>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
<script type="text/javascript">

    function check() {

        var searchvalue = $('#search').val();
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Searched Application</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/search?sea=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>"+
                                    "<img data-src='holder.js/300x200' src="+item.Icon+" >"+
                                      "<div class='caption'>"+
                                        "<h4>"+item.aname+"</h4>"+
                                        " <p><span class='badge'>"+item.type+"</span>"+
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +
                                                @*+"@Html.ActionLink('Details', 'AppDetails', new { id = "+item.Id+"})"+*@
                                                    "</form></p>"+
                                                                "</div>"+
                                                         "</div>"+
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#smn').append("<h4 style='margin-left:217px'> Not Found!</h4>" +
          "<div class='row span9' style='margin-left:175px' >"
      + "</div>");
                return true;;

            }
        });
    }

    function getGames() {

        var searchvalue = "Games";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Games</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchCatagories?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +
                                                
                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').append("Your required Application is not available");
                return true;;

            }
        });
    }
    function getSocial() {

        var searchvalue = "Social";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Social Applications</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchCatagories?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }

    function getBusiness() {

        var searchvalue = "Business";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Business Applications</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchCatagories?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }
    function getEducation() {

        var searchvalue = "Education";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Education Applications</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchCatagories?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }
    function getHealth() {

        var searchvalue = "Health";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Health Applications</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchCatagories?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }
    function getFree() {

        var searchvalue = "free";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Free Applications</h4>" +
           "<div class='row span9' style='margin-left:175px' id='smn' >"
       + "</div>");
        $.getJSON("/Account/searchTypes?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append("<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }
    function getPaid() {

        var searchvalue = "paid";
        $('#mn').empty();
        $('#mn').append("<h4 style='margin-left:217px'> Paid Applications</h4>"+
            "<div class='row span9' style='margin-left:175px' id='smn' >"
        +"</div>");
        $.getJSON("/Account/searchTypes?cata=" + searchvalue, function (data123) {
            if (data123) {
                $.each(data123, function (i, item) {
                    $('#smn').append(
                    "<div class='col-sm-6 col-md-3' style='margin-left:25px'>" +
                                    "<div class='thumbnail'>" +
                                    "<img data-src='holder.js/300x200' src=" + item.Icon + " >" +
                                      "<div class='caption'>" +
                                        "<h4>" + item.aname + "</h4>" +
                                        " <p><span class='badge'>" + item.type + "</span>" +
                                            "<form action='AppDetails/" + item.Id + "' method='post' role='form'>" +
                                             "<input type='submit' value='Detail' class='btn btn-info'>" +

                                                    "</form></p>" +
                                                                "</div>" +
                                                         "</div>" +
                                          "  </div>");
                });
                return false;
            }
            else {

                $('#mn').text("Your required Application is not available");
                return true;;

            }
        });
    }
    
</script>

</head>
 
<body>

    <nav class="navbar navbar-fixed-top navbar-default" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="~/Account/Index"><img style="height:50px; width:70px" src="~/Images/logo1.jpg"><strong>APPSTOCKER</strong></a>
        </div>

        <div class="collapse navbar-collapse navbar-ex1-collapse">
          <ul class="nav navbar-nav navbar-right">

            <li><a href="~/Account/Index"><strong>Home</strong></a></li>
            <li><a href="~/Contact/Index"><strong>Contact Us</strong></a></li>
            <li> 
              <form class="navbar-form navbar-left" role="search" action="~/Account/search">
                <div class="form-group">
                    <input type="text" class="form-control" id="search" placeholder="Search">
                </div>
                <button type="button" class="btn btn-info" onclick="check();">Search</button>
              </form>
            </li>
       
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><strong>More</strong><b class="caret"></b></a>
        <ul class="dropdown-menu pull-right">
          <li><a href="/Account/ManageProfile">Manage Profile</a></li>
            <li><a href="/Details/AddApplication">Add Application</a></li>
          <li class="divider"></li>
          <li><a href="/Home/Index">Logout</a></li>
        </ul>
      </li>
        </ul>

        </div>
      </div>
    </nav>


    <div class="container col-sm-6 col-sm-3">
  	<div class="row">
      <div class="col-sm-2">
          <ul id="sidebar" class="nav nav-stacked affix well col-sm-2">
            <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><strong>Catagories</strong><b class="caret"></b></a>
        <ul class="dropdown-menu pull-right">
          <li><a  onclick="getGames();">Games</a></li>
           <li><a  onclick="getSocial();" >Social</a></li>
            <li><a  onclick="getEducation();">Education</a></li>
            <li><a  onclick="getBusiness();">Business</a></li>
            <li><a  onclick="getHealth();">Health</a></li>
        </ul>
      </li>
            <li><a  onclick="getFree();"> <strong>Free Application</strong></a></li>
            <li><a  onclick="getPaid();"> <strong>Paid Application</strong></a></li>
            <li><a href="#"> <strong> Help </strong></a></li>
        </ul>
      </div>
          </div>
        </div>
    @RenderBody()



    @Scripts.Render("~/bundles/jquery")
    @RenderSection("scripts", required: false)


 <footer>
<div class="foot-fixed-bottom ">
<div class="container">
   <hr>
    <ul class=" nav navbar-nav">
<li><a href="#"> <strong>About Us</strong></a></li>
    <li><a href="#"> <strong>Terms and Conditions</strong></a></li>
    <li><a href="#"><strong> Privacy Policy</strong> </a></li>
    <li><a href="#"><strong> Developers</strong> </a></li>
     <li><a><strong>©2014 AppStocke</strong>r</a></li>
        </ul>
    
</div>
</div>
</footer>
</body> 
</html>